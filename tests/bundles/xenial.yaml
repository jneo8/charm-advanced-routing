series: xenial
description: "charm-advanced-routing test bundle"
machines:
  "0":
    constraints: "arch=amd64"
relations:
  - - "testhost:juju-info"
    - "charm-advanced-routing:juju-info"
applications:
  testhost:
    charm: "ubuntu"
    options:
      hostname: "testhost"
    num_units: 1
    to:
      - "0"
  charm-advanced-routing:
    charm: "../.."
    series: xenial  # Cf. lp:1804063
    num_units: 0
    options:
      cidr: 10.9.9.0/24
      gateway: 10.9.9.1
